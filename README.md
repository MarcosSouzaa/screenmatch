# 🎬 ScreenMatch Project: Programação Orientada a Objetos (POO) em Java

## 🌟 Status do Projeto
➡️ **Fase 2: Implementação Orientada a Objetos (POO) - EM ANDAMENTO**

## 🎯 Objetivo

Este repositório é dedicado à **Fase de Programação Orientada a Objetos (POO)** do meu estudo em Java, utilizando o projeto **ScreenMatch** como base prática. O objetivo é transformar um conceito de catálogo de filmes e séries em um código modular, escalável e de fácil manutenção, aplicando os quatro pilares da POO.

## 💡 Pilares da POO e Conceitos em Foco

O desenvolvimento deste projeto será guiado pela implementação e consolidação dos seguintes conceitos:

### 1. Classes e Objetos
* Criação das classes primárias (`Filme`, `Serie`, `Titulo`) para modelar entidades do mundo real.
* Instanciação de objetos (como um filme específico) a partir dessas classes.

### 2. Encapsulamento
* Declaração de atributos como `private` (privados) para proteger o estado interno dos objetos (ex: o nome do filme, a duração, o ano de lançamento).
* Criação de métodos **Getters** e **Setters** para controlar o acesso e a modificação desses atributos.

### 3. Abstração
* Foco em expor apenas as funcionalidades essenciais para o usuário (ex: `exibeFichaTecnica()`) e ocultar a complexidade interna do objeto.

### 4. Herança e Polimorfismo
* Utilização de uma classe base (`Titulo`) para herdar atributos comuns entre `Filme` e `Serie`.
* Implementação de métodos que se comportam de maneira diferente dependendo do tipo de objeto (Polimorfismo).

## ⚙️ Estrutura do Projeto e Execução

O projeto é estruturado em pacotes (packages) e classes. A execução principal ocorrerá através de uma classe `Main` dedicada a testar a interação entre os objetos criados.

1.  **Clone o Repositório:**
    ```bash
    git clone [LINK_DO_SEU_NOVO_REPOSITORIO]
    ```
2.  **Abra no IntelliJ IDEA:**
    * No IntelliJ, selecione **File** -> **Open** e aponte para a pasta clonada.
3.  **Execute:**
    * Execute o método `main` da classe principal para ver os objetos sendo criados e interagindo.

---
